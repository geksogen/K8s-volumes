apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myclaim
  namespace: guestbook
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
  volumeName: my-pv